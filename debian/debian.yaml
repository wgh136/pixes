flutter_app: 
  command: pixes
  arch: x64
  parent: /usr/local/lib
  nonInteractive: true
  execFieldCodes: u

control:
  Package: pixes
  Version: {{Version}}
  Architecture: amd64
  Priority: optional
  Depends:
  Maintainer: nyne
  Description: Unofficial pixiv application
  
#options:
#  exec_out_dir: debian/packages